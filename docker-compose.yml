services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: ahmad-alshawakri
      POSTGRES_PASSWORD: blgMLI$300
      POSTGRES_DB: flightai
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  pgdata:
